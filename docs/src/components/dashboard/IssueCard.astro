<script>
  // Logic lives in lib/dashboard/createIssueCard so the board (314) can
  // import it directly — a client <script> tag's exports aren't reachable
  // from other files once bundled, only the underlying module's are.
  export { createIssueCard } from '../../lib/dashboard/createIssueCard';
</script>

<style is:global>
  .issue-card {
    display: block;
    padding: 0.75rem;
    border: 1px solid var(--sl-color-hairline);
    border-radius: 0.5rem;
    background: var(--sl-color-bg);
    color: inherit;
    text-decoration: none;
    font-family: var(--__sl-font);
    transition: border-color 0.15s ease;
  }

  .issue-card:hover,
  .issue-card:focus-visible {
    border-color: var(--sl-color-accent);
  }

  .issue-card:focus-visible {
    outline: 2px solid var(--sl-color-accent);
    outline-offset: 2px;
  }

  .issue-number {
    display: block;
    font-family: var(--__sl-font-mono);
    font-size: 0.75rem;
    color: var(--sl-color-gray-3);
  }

  .issue-title {
    margin: 0.25rem 0 0;
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--sl-color-white);
  }

  .issue-excerpt {
    margin: 0.25rem 0 0;
    font-size: 0.8rem;
    color: var(--sl-color-gray-2);
  }

  .issue-labels {
    display: flex;
    flex-wrap: wrap;
    gap: 0.25rem;
    margin-top: 0.5rem;
  }

  .label {
    display: inline-block;
    padding: 0.1rem 0.5rem;
    border-radius: 999px;
    font-size: 0.7rem;
    font-weight: 500;
    line-height: 1.4;
  }

  .label-overflow {
    background: var(--sl-color-gray-5);
    color: var(--sl-color-gray-2);
  }
</style>
